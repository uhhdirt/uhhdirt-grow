import React from 'react';
import ReactDOM from 'react-dom/client';
import GrowTracker from './GrowTracker.jsx';
import './index.css';
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode><GrowTracker /></React.StrictMode>
);
